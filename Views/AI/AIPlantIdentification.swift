import SwiftUI

/// Stub kept for compatibility. The real identification UI is in PlantIdentificationView.
struct AIPlantIdentification: View {
    var body: some View {
        PlantIdentificationView()
    }
}

#Preview {
    AIPlantIdentification()
}

